import { defaultTheme } from '@react-native-styled-system/core';

const theme = {
  ...defaultTheme,
  space: {
    ...defaultTheme.space,
    sfTop: 0,
    sfRight: 0,
    sfBottom: 0,
    sfLeft: 0,
  },
};

export default theme;
